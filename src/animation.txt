<!DOCTYPE html>
<html>
<head>
  <title>Neoma Digital Studio</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      padding: 25px;
    }
    .title {
      color: #5C6AC4;
    }
    img {
      width: 400px;
      height: 100%;
      object-fit: cover;
    }
    p, a {
      text-decoration: none;
      font-size: 13px;
      color: black;
    }
    .counter {
      height: 40px;
      width: 80px;
      position: absolute;
      top: 50%;
      left: 50%;
      background-color: blue;
      transform: translate(-50%, -50%);
      clip-path: polygon(0% 0%, 100% 0%, 100% 100%, 0% 100%);
      text-align: center;
      z-index: 0;
    }
    .counter p {
      color: white;
      line-height: 40px;
    }
    .hero {
      position: absolute;
      top: 50%;
      left: 50%;
      height: 100vh;
      width: 100vw;
      display: flex;
      flex-direction: column;
      font-family: Arial, sans-serif;
      background-color: #0c0c0c;
      align-items: center;
      transform: translate(-50%, -50%) scale(0.7);
      clip-path: polygon(0% 100%, 100% 100%, 100% 100%, 0% 100%);
      will-change: transform;
      z-index: 1;
    }
    .overlay {
      position: absolute;
      top: 0;
      left: 0;
      height: 100vh;
      width: 100vw;
      background-color: #dad3d3;
      z-index: 2;
      clip-path: polygon(0 0, 100% 0, 100% 100%, 0 100%);
    }
    nav {
      width: 100%;
      padding: 20px;
      display: flex;
      justify-content: space-between;
    }
    .nav-col {
      display: flex;
      flex-direction: column;
      gap: 10px;
    }
    .nav-item {
      display: flex;
      gap: 20px;
    }
    .header {
      margin: 40px 0;
    }
    .header h1 {
      font-size: 48px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="counter"><p>0</p></div>
    <section class="hero">
      <div class="overlay"></div>
      <nav>
        <div class="nav-col">
          <div class="nav-item">
            <p>Neoma</p>
          </div>
          <div class="nav-item">
            <p>digital studio</p>
          </div>
        </div>
        <div class="nav-col">
          <div class="nav-item">
            <a href="#">work</a>
            <a href="home">home</a>
            <a href="blog">blog</a>
          </div>
          <div class="nav-item">
            <a href="new">new</a>
            <a href="personal">personal</a>
            <a href="teal">teal</a>
          </div>
          <div class="nav-item">
            <p>Montreal</p>
          </div>
        </div>
      </nav>
      <div class="header">
        <h1>Neoma</h1>
      </div>
      <div class="hero-img">
        <img src="/api/placeholder/400/320" alt="hero-image" />
      </div>
    </section>
  </div>

  <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.3/gsap.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/CustomEase.min.js"></script>
  <script>
    function animateCounter() {
      const counterElement = document.querySelector(".counter p");
      let currentValue = 0;
      const updateInterval = 300;
      const maxDuration = 2000;
      const endValue = 100;
      const startTime = Date.now();

      function updateCounter() {
        const elapsedTime = Date.now() - startTime;
        
        if (elapsedTime < maxDuration) {
          currentValue = Math.min(
            currentValue + Math.floor(Math.random() * 30) + 5,
            endValue
          );
          
          counterElement.textContent = currentValue;
          setTimeout(updateCounter, updateInterval);
        } else {
          counterElement.textContent = currentValue;
          
          setTimeout(() => {
            gsap.to(counterElement, {
              y: -20,
              duration: 1,
              ease: "power3.inOut",
              onStart: revealLandingPage,
            });
          }, 500);
        }
      }
      updateCounter();
    }

    function revealLandingPage() {
      // First reveal the hero section
      gsap.to(".hero", {
        clipPath: "polygon(0% 100%, 100% 100%, 100% 0%, 0% 0%)",
        duration: 2,
        ease: "hop",
        onStart: () => {
          // Animate the hero scaling simultaneously
          gsap.to(".hero", {
            transform: "translate(-50%, -50%) scale(1)",
            duration: 2.25,
            ease: "power3.inOut",
            delay: 0.25,
          });
          
          // Animate the overlay removal
          gsap.to(".overlay", {
            clipPath: "polygon(0% 0%, 100% 0%, 100% 0%, 0% 0%)",
            duration: 2,
            ease: "power3.inOut",
            delay: 0.5, // Slight delay to create a nice sequence
          });
        },
      });
    }

    // Initial setup
    document.addEventListener("DOMContentLoaded", () => {
      // Register the CustomEase plugin and create the "hop" ease
      gsap.registerPlugin(CustomEase);
      CustomEase.create(
        "hop",
        "M0,0 C0.29,0 0.348,0.05 0.422,0.134 0.494,0.217 0.484,0.355 0.5,0.5 0.518,0.662 0.515,0.793 0.596,0.876 0.701,0.983 0.72,0.987 1,1"
      );
      
      // Start the animation sequence
      gsap.to(".counter p", {
        y: 0,
        duration: 1,
        ease: "power3.out",
        delay: 1,
        onComplete: animateCounter,
      });
    });
  </script>
</body>
</html>
